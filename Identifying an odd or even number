#include <stdio.h>
int main ()
{
    int a,i;
    for(i=1;i<=5;i++)
    {
    printf ("Enter a number:");
    scanf("%d",&a);
if (a>=1 && a<=500)
    if (a%2==0)
        printf("The number is even.\n");
    else
        printf("The number is odd.\n");
else 
    printf("Wrong Input.");
    }
}
